# hf-llm/requirements.txt
# HF Spaces T4 small (16GB VRAM) 部署
# 三合一：Embedding + Reranker + Qwen3-4B Chat (AWQ 量化常驻)

# ---------- Web 层 ----------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0
gradio>=4.44.0

# ---------- 深度学习 & 推理 ----------
# Torch 由 Space 的 base image 提供 (nvidia CUDA 12.1)，这里只锁最低版本
torch>=2.3.0
transformers>=4.51.0            # Qwen3 系列必需 >= 4.51
tokenizers>=0.19.0
accelerate>=0.30.0
safetensors>=0.4.3

# Embedding + Reranker 走 sentence-transformers 官方链路（Qwen3 官方推荐）
sentence-transformers>=3.0.0

# ---------- Qwen3-4B AWQ 量化推理 ----------
# autoawq: AWQ 量化模型 GPU 推理内核；比 GPTQ 快 30%+
autoawq>=0.2.6

# ---------- 工具 ----------
python-dotenv>=1.0.0
httpx>=0.27.0
numpy>=1.26.0
