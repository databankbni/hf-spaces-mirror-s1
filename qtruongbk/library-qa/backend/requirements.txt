# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# PDF extraction
pdfplumber==0.11.0
pymupdf>=1.24.0         # Render PDF page → image (không cần poppler)
pytesseract==0.3.13     # OCR wrapper (cần tesseract-ocr system package)

# Embeddings
sentence-transformers==3.0.1
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.0; platform_system == "Darwin"
torch==2.3.0+cpu; platform_system != "Darwin"


# Vector store
faiss-cpu==1.13.0

# Google Drive
gdown>=5.1.0              # Tải file/folder public không cần credentials
google-api-python-client==2.131.0
google-auth==2.29.0
google-auth-httplib2==0.2.0
google-auth-oauthlib>=1.2.0  # OAuth user flow (Gmail) — push_index dùng quota của user

# LLM (DeepSeek via OpenAI-compatible API)
openai==1.30.1
httpx==0.27.0  # openai 1.30.x yêu cầu httpx < 0.28

# Utilities
numpy>=1.26.4
python-dotenv==1.0.1
rank-bm25>=0.2.2
