# FYND 서빙 런타임 의존성 (app.py + src/)
# 로컬 검증 환경과 동일한 메이저 버전으로 핀 고정 — transformers 5.x는
# CLIP get_*_features 반환 타입이 달라져 배포에서 깨지는 것을 확인함
flask>=3.0.0,<4
flask-cors>=4.0.0
werkzeug>=3.0.0
numpy>=1.26.0,<3
Pillow>=10.0.0
torch>=2.2.0,<3
sentence-transformers>=5.0.0,<6
transformers>=4.56.0,<5
huggingface-hub>=0.30.0,<1.0
# scripts/ (분류·임베딩 재생성) 실행 시 사용
tqdm>=4.66.0
