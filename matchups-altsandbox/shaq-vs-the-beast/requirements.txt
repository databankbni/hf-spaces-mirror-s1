# Runtime dependencies for the API + UI server (Hugging Face Docker image).
# pybaseball is intentionally omitted — it is only needed for data ingestion,
# which is done offline; the served DB is bundled into the image.
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
requests>=2.31
click>=8.1
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.5
# Powers the homepage assistant. The feature is gated on ANTHROPIC_API_KEY
# being set, so the dependency is harmless on a deployment that never uses it.
anthropic>=0.69
