# Development / offline work: everything the served image needs, plus the
# heavy scientific stack used only for data ingestion, calibrator fitting and
# backtests. The Hugging Face runtime image installs requirements.txt alone —
# see the note there — so these must never become import-time dependencies of
# the serving path.
-r requirements.txt

pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
