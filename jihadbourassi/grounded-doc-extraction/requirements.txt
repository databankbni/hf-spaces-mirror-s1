# ---- Phase 0 dependencies -------------------------------------------------
# Pinned exactly: the same versions must install on my laptop and in the Space.
# NOTE: `gradio` is NOT listed here. On a Gradio Space the version comes from
# `sdk_version` in README.md; listing it twice can cause conflicts.

# PDF -> pixels
pypdfium2==5.6.0
pillow==11.3.0

# OCR: PP-OCRv4 detection+recognition models shipped INSIDE the wheel (~16 MB),
# so there is no model download at runtime and no external model host to fail.
rapidocr-onnxruntime==1.4.4
onnxruntime==1.24.4
numpy==2.4.4

# LLM + embeddings (downloaded from the Hub at runtime, cached on disk)
transformers==5.15.0
accelerate==1.14.0
huggingface_hub==1.27.0
# torch is intentionally NOT pinned here: the ZeroGPU base image ships a
# CUDA-matched build. Pinning it can force a multi-GB reinstall or an
# unsupported version. Verified in Phase 0 from the build logs.

# ZeroGPU decorator (preinstalled on ZeroGPU Spaces; listed for local parity)
spaces==0.51.1

# reporting
psutil==7.1.3
