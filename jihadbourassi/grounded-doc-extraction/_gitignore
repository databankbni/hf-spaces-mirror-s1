# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/

# --- secrets: never commit these ---
.env.*
*.pem
*.key
secrets.*
.hf_token

# --- model weights & caches (large, redownloadable, must not be in git) ---
.cache/
hf_cache/
*.onnx
*.safetensors
*.bin
*.gguf
*.pt
*.pth

# --- data: keep the repo free of scans until we explicitly whitelist examples ---
data/
downloads/
*.pdf
!examples/*.pdf

# --- generated ---
outputs/
benchmark/results/*.json
.gradio/
.pytest_cache/
.ipynb_checkpoints/

# --- Phase 1 audit metadata: version the CSV, never the scans ---
!data/
!data/audit/
!data/audit/*.csv
