# Crusoe LLM support merged into Pipecat main on 2026-07-16 (pipecat-ai/pipecat#5024).
# Install from git until it lands in a PyPI release (> 1.5.0).
pipecat-ai[kokoro,runner,silero,webrtc,whisper] @ git+https://github.com/pipecat-ai/pipecat.git
openai>=1.74
httpx>=0.27

# Pipecat's whisper module requires MLX on Apple Silicon
mlx-whisper~=0.4.2; sys_platform == "darwin" and platform_machine == "arm64"
