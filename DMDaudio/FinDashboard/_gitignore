__pycache__/
*.pyc
.venv/
.pytest_cache/
.DS_Store
.streamlit/secrets.toml
saved_sectors.json
# DB lives in a separate HF Dataset (DMDaudio/findashboard-data) to keep the
# Space repo under its 1 GB LFS quota. The Space downloads it on startup.
georgian-financials-v2.db
# Local-only stash files that occasionally land in this dir by mistake —
# HF rejects binaries without xet, so keep them out of the deploy.
Cookie.txt
NACE/
Temp/
