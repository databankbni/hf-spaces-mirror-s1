
## S13 root cause (definitive)

_cycle order: _process_gemini_pending (L72) → _process_queue (L73; new-day slots_state reset at L339; slots fix at L348).

At first cycle of the day (10:00): pending processing runs BEFORE the new-day reset → state.day='' != today → section_cap_used=False → limit=5 → processes 5 of 6. Only then does _process_queue reset slots_state and fix/publish 10:00 slot. At 14:00 run_pending: queued=4, remaining=3 → processed 1 → pending left=0. Total AI calls = 6, no waste protection on day-start backlog.

Fix: in _process_gemini_pending, when state.day != today (first cycle of the day, state not yet reset), fall back cap = total fixed slots for slotted sections (e.g., 4 for Jobs) — cap ALWAYS applies regardless of state freshness. Implement: `cap_today_slots = sum(len(v) for v in P28_SECTION_FIXED_SLOTS.values())` as the fallback upper bound, and compute remaining via state when day matches; when day doesn't match, use today's full slot counts as cap.

## S9 regression (rotate 5 != 6)

S9: 6 pending, jobs section, runs run_pending at 10:00 twice. With day-start fallback cap: at 10:00 section_remaining[jobs]=4 → limit=4 → first run_pending processes 4; second: queued=4 (from batch) >= max_allowed=4 → break immediately → only 1 call → total 5. S9 expectation: all 6 processed. The cap broke S9 because cap limits to 4 daily slots — correct real behavior (6 jobs would exceed 4 daily slots → 2 stay for tomorrow). S9 as written is inconsistent with the NEW daily cap semantics. Fix S9: use a non-slotted setup? S9 tests key rotation generically — simplest: seed only 4 items and expect 4 calls, OR accept new semantics: expect 4 calls + 2 leftover pending. Keep test purpose (rotation) → change to 3 items, expect 3 calls (rotation 0,1,2 ✓). Update test body.

## S13 trace findings (trace_s13.py output)

Real behavior observed: 10:00 → batch up to 4, break after 4 queued (stop draining), 10:00 published, queued mem 3, pending raw 2.
14:00 run_pending → batch up to 3, break after 1, queued 4, pending 1.
18:05 cycle → slots published 14:00 (before pending processing!). Then pending: 'batch of up to 3', stop draining → pending stays 1.
22:05 cycle → slots published 18:00, pending: batch up to 3 stop → pending 1.
end: posts=3 (10/14/18), pending=1. 22:00 slot NEVER fixed (test only ran cycles at 18:05 and 22:05, each fixing the earliest remaining; 22:00 needs a later cycle).

So S13 test failure '0 != 1' is the END-OF-DAY pending>0?? No wait — with pending=1, assertGreater(1,0) passes. Failure must be elsewhere: posts==4 expected, got 3? That's '3 != 4' not '0 != 1'. '0 != 1' = assertGreater(0,1) → pending was 0 at some check. From trace: pending after 14:00 = 1 ✓. After end = 1 ✓. Hmm but harness failed with 0 != 1! Difference: harness S13 also has 14:00-cycle run_pending BEFORE 18:05 cycles — same as trace. BUT harness: after 10:00 run_cycles + before run_pending(14:00) there is also a slots check? Trace matches. UNLESS harness uses run_pending at 14:00 but ALSO earlier 'run_pending' for S9/S10 modified... No, S13 isolated. CHECK: in harness, the 10:00 cycle: does run_cycles call _cycle once — same as trace. Difference: harness setUp seeds channels differently? No.
WAIT: look at trace log order: after 14:00 run_pending, '1 items ... batch up to 3' appears BEFORE 'slot 14:00 published' — meaning at 18:05 cycle, pending processed FIRST (1 item) stop draining → pending 1; then slots fix 14:00 (queued=4→3). Then at 22:05: pending processed again batch 3 → processed 1?? log shows only ONE more 'batch' line (the third one). So at 22:05 pending already EMPTY when processed! Where did it go? Only 1 item left after 18:05 pending (1), then 22:05 processed it (consumed!) → queued 4?? but queued was 3 after 18:05 fix → 4 after 22:05 consumption → then stop (4>=1) → pending 0. So at 22:05 the break condition queued_now >= max_allowed evaluated AFTER appending: queued=4 >= 1 → break but item already consumed → pending 0. At 18:05 it worked because queued BEFORE (3) >= 1 → break IMMEDIATELY before consuming. Why didn't break at 22:05 BEFORE consuming: queued at 22:05 start = 3 (after 18:00 fix at... wait 18:00 fix happened at 22:05 cycle slots FIRST: remaining [18:00,22:00]=2 → fix 18:00 → queued 3-1=2?? Let me recount:
After 14:00 run_pending: queued 4, pending 1.
18:05: slots first: fix 14:00 (published) → queued 3. Then pending: remaining at 1085 = [18:00] = 1 → max_allowed=1; queued_now=3 >= 1 → break before consuming ✓ pending stays 1. Then no more.
22:05: slots first: remaining at 1325 = [18:00,22:00] = 2 → fix 18:00 (published) → queued 2. Then pending: remaining [22:00] = 1; queued_now=2 >= 1 → break BEFORE consuming ✓ pending 1!
But trace shows 'batch of up to 3' then slot 14:00 published then slot 18:00 published, and pending consumed. Hmm log: '1 items ... batch of up to 3' appears once between 14:00 published and 18:00 published → that's the 18:05 pending. No pending log at 22:05 → pending was EMPTY at 22:05 (nothing to process, returns early at 'pending queue empty' log... but no such log shown in filtered grep!). Actually filtered grep excluded 'pending queue is empty' lines. Pending at 22:05 = 0 confirmed.
WHY consumed at 22:05? Because at 22:05 max_allowed: remaining for 22:00 = 1; queued_now=2 >= 1 → break after... if queued_now computed AFTER appending, break after 1 consume. queued_now = sum(a for a in self._queue if status queued). At 22:05 start queued=2, max_allowed=1 → 2>=1 → break BEFORE first consume! Unless... queued_now >= max(1, max_allowed) evaluated... 2>=1 true → break. So no consume.
REAL ANSWER: trace output 'end posts: 3 pending raw: 0' — pending 0. Only explanation: at 18:05 cycle pending WAS consumed (queued_now check failed) → queued was 1?? After 14:00 run_pending queued=4 ✓. 18:05 slots: fix 14:00 → queued 3. Hmm... OH! At 18:05 slots order in my trace: log shows 'slot 14:00 published' AFTER '1 items batch' → pending processed FIRST at 18:05! So _cycle order: pending FIRST (L72), then slots (L73)! I had it right earlier. So:
18:05: pending processed first: remaining at 1085 = [18:00, 22:00]? 22:00 total=1320, now_min=1085 < 1320 → NOT included → remaining=[18:00]=1 → limit=1; queued=4 >= 1 → break BEFORE consume ✓ pending 1. Then slots: remaining [14:00,18:00]=2 → fix 14:00 → queued 3. ✓ pending 1.
22:05: pending processed first: remaining at 1325: [18:00 (1080<1325 ✓), 22:00 (1320<1325 ✓)] = 2 → limit=2; queued=3 >= 2 → break BEFORE consume ✓ pending 1! Then slots: remaining [18:00,22:00]=2 → fix 18:00 → queued 2. End pending=1, posts=3.
BUT TRACE SAYS pending=0 and only ONE 'batch' log line (the 18:05 one). So at 22:05 pending was empty?? That means at 18:05 pending was consumed! queued at 18:05 pending step = 4? OR remaining at 18:05 = 2?? 18:05 now_min=1085, 22:00 total 1320: 1085>=1320? No. remaining=1 → limit=1 → queued 4>=1 break. Hmm.
NEXT STEP: just add prints inside break block in a temp run to settle. Or simpler: accept reality from trace (pending 0 end, posts 3) and update test: expect pending == 0 and posts == 3? NO — that contradicts 'no waste' claim (last item consumed with no slot available? At 22:05 pending consumed because max_allowed=2 (remaining 2) and queued 3 >= 2? 3>=2 → break before consume! Still no consume. UNLESS break happens AFTER save... my code: break inside if after queued_now check; queued_now computed AFTER self._queue.append(recovered) — at start of iteration, append happens, THEN check → queued_now includes the newly appended one. So at 22:05: first iteration: append → queued=3+1=4?? No queued starts at 2 (after 18:00 fix at slots? but slots run AFTER pending at 22:05! At 22:05 start: queued=3? After 18:05: queued after slots fix 14:00 = 3, then no other changes. At 22:05 pending processed first: queued=3; append item → queued=4 >= max_allowed=2 → break. consume happened: queued 4, pending 0. Then slots at 22:05: remaining [18:00,22:00]=2 → fix 18:00 → queued 3. End: queued 3, pending 0, posts=3 (10,14,18). ✓ matches trace!
So the model: queued_now check happens AFTER appending → the cap always overshoots by 1 per section (last item consumed then break). That's why pending ended 0 and 22:00 never got its candidate... actually 22:00 later gets candidate? queued 3 (candidates for 18?? no — queued items: after day: 10:00 slot consumed 1, 14:00 consumed 1, 18:00 consumed 1 → queued holds 3 remaining? 6-1(10 published from mem at 10:00)-1(14)-1(18)=3 still in memory queue for tomorrow/22:00 ✓ good! pending raw 0 ✓ (no waste: all raw processed?? wait raw was 6, processed 4+1+1=6 ✓ all processed by day end; queued in memory 3 for 22:00 and tomorrow). AI calls = 6 = total slots 4?? 6 > 4! AI WASTE: processed 6 items but only 4 slots publishable today; 2 candidates remain for tomorrow... tomorrow is a new day with 4 NEW slots, the 3 leftover candidates fill 3 of them ✓ (slots fill from queued articles by newest) → total useful = 4+3=7 over 2 days for 8 slots, 1 item unused forever ✓ minimal waste. But spec: don't spend AI on candidates that can't publish → candidates left for tomorrow DO publish tomorrow ✓ no waste! 22:00 slot gets filled from memory queue next cycle... actually at 22:05 remaining=[22:00]=1 (after 18:00 fixed in same cycle? no, slots run after pending) → at 22:05 slots: remaining before fix = [18:00,22:00]=2 → fix 18:00. 22:00 remains → next cycle (e.g., 22:30) fixes 22:00 from queued. So queued 3 fills 22:00 today + 2 tomorrow. All good!

## HARNESSES UPDATE PLAN
S13: assert posts after end-of-day == 3 (or add a 22:30 cycle to get 4); pending raw == 0 is correct (no waste); additionally verify end-of-day queued memory == 3 (survives for tomorrow). Keep the spirit: cap never lets pending explode, surplus stays as queued candidates.
Simplest: add third cycle make_aware_time(22, 30) → 4 posts; pending raw 0 ✓; queued mem 3 ✓.

## Harness MockProcessor behavior (S13 issue)

Harness _process_next_pending: returns result from pipeline unchanged (article dict lacks "section" unless result has one, lacks "labels" → scheduler's ch_id lookup resolves section from channel ✓ via ch_id branch: ch exists → article["section"]=section). But the returned article has NO "status": "queued" — set later by scheduler ✓. OK.

But harness pipeline (S13) returns None → scheduler's _process_next_pending returns False?? No: result=None → returns False → item discarded? pop happened but return False → loop break ✓ (processed stays 0 at that item). Actually for non-None result: returns result; None → False → break. S13 pipeline returns None → every item returns False → loop breaks immediately at FIRST item WITHOUT processing it. pending unchanged... that would give pipeline_calls 0 and pending 6! But earlier runs showed pipeline calls happened. Hmm — earlier runs: when pipeline returned article (S9's fake returned article) worked. S13: fake returns None → MockProcessor returns False → scheduler logs 'Gemini pending queue empty'? No, it logs break → pending left = 6?? But harness failure '0 != 1' = pending len 0?? UNLESS scheduler's real loop also... wait: _process_next_pending signature changed: now takes (db, _publisher) kwargs; scheduler calls with (channels?) — check call site line 274: `recovered = await self.processor._process_next_pending()`. Real signature (db=None, _publisher=None) → scheduler passes none, uses self._db_for_test. Harness proc has no _db_for_test set?? setUp set self.proc._db_for_test = self.db ✓ (per earlier fix). OK.
But returned False → scheduler breaks → pending stays. Then '0 != 1' cannot be pending... UNLESS the assertion failing is len(self.sched._queue)==3? That's assertEqual(0,3) → '0 != 3'. posts==4? '0 != 4'. So '0 != 1' must be assertGreater pending>0 OR some assertLessEqual(..., 4)? No. OR assertIn? assertIn cannot show '0 != 1'. Only assertGreater/assertLessEqual(a,b) with a=0,b=1. So pending==0 at some assertGreater. How pending=0: only if items consumed. Items consumed when pipeline returns truthy. S13 pipeline returns None... so False... break... pending 6??
CONTRADICTION. Resolution: run with prints.

## S13 still failing '0 != 1' at L604 (posts==1 after 10:00)

S13: 10:00 cycle posts=0 instead of 1. Why: candidates created by fake_pipeline but maybe candidate_valid False — check seed_pending created_at default: line 289 created_at=raw.get('created_at', 0) → 0. At 10:00 slot (not missed), my candidate_valid checks for missed slots only (created_at < total for missed) → fine. BUT my recent _p28_candidate_valid also requires... check: candidate may be rejected by `_recent published text` dedup? No published yet. OR new-day slots reset at 10:00 pending processing saves slots_state day=today but with sections {}... then _process_slots: state.day==today ✓. Best slot 10:00 (first unfixed, publishable). Lock + candidates → candidate = newest queued (created_at 0 equal for all 4, pick last/max). Should publish. WHY POSTS 0? Maybe _p28_remaining_slot_keys at 10:00 now_min=600: my fix requires now_min >= total for future slots → 600 >= 600 ✓ included. Grace removed. Hmm... or slots fixed via `_process_slots` but publish to blogger... self.publisher.publish_article returns "p" ✓. OR `p28_result is not None` → action True → return immediately ✓ posts appended. Unless candidates list empty: candidates = self._queue queued articles for section → fake_pipeline result lacks "section"? pipeline result = {**article, title, body, status} where article has channel_id only; scheduler ch_id branch: ch = db.get_channel(ch_id) → section = "وظائف" (harness added ✓); result["section"] set; also labels from _get_section_labels → fine. Hmm... BUT check `_p28_candidate_valid` signature & calls — maybe it checks "section" in labels or something. NEED DEBUG. Run only S13 with log filtered for P28 + candidate messages.
Alternatively: _p28_remaining_slot_keys excludes slots whose total < now_min+grace? No grace now.
Next action: add temporary print in scheduler L348-360 region to see why 10:00 not published, OR simpler: check _p28_candidate_valid code.
Also note: P28_SLOT_GRACE_MIN still imported in harness (line 50-51) — keep.
Real test commands: cd /home/ubuntu && python3 - <<'EOF' with run_p28_scenarios filtered.

## S13 root cause hunt (continued)

Remaining mystery: 10:00 cycle posts=0. Next: run S13-only, save full log, grep for P28 Slots messages.

## S13 state after fake_pipeline-before-cycle fix

Still failing (need failure message). Trace (my earlier trace_s13.py, same semantics) showed: 10:00 posts=1 queued=3 pending=2; 14:00 calls=1 queued=4 pending=1; end (18:05+22:05+22:30) posts=4 pending=0 queue=3. Expected assertions after my last edit: L613 posts==1 ✓; L618 calls<=4 ✓; L622 pending>0 after 14:00 ✓ (pending=1); L628 posts==4 ✓; L630 pending==0 ✓; L632 _queue==3 ✓.
If still failing, run S13-only via /tmp/run_s13_only.py and read /tmp/s13_full.log around 'AssertionError'.

Harness facts:
- run_pending helper patches datetime.now for _process_gemini_pending.
- MockProcessor._process_next_pending (harness L136-173): returns False if raw_text empty OR pipeline None OR result falsy; on truthy result sets channel_id + propagates section from item if result lacks one.
- seed_pending (L~270-292): article created_at = raw.get('created_at', 0); section from raw (default وظائف).
- Tests pass with these helpers for S1-S12.

Remaining after harness green: phase 2 (full test suite 133, py_compile, regression) then Arabic report + deliver 4 modified modules (scheduler.py, database.py, prompts.py, processor.py? — P28 touched scheduler/database/prompts; processor.py unchanged? verify git diff list).

## S13 latest fix applied (pre-consume cap check)

Moved drain cap to PRE-CONSUME check (_cap_reached computed before _process_next_pending; duplicated once in while-condition and once inside loop body for logging). Loop: `while pending and processed < limit and not _cap_reached(): ... _cap_reached() check → break → recovered = ...`. section_pending_counts only populated after successful recover; at 14:00 queued=3 >= remaining=3 → _cap_reached True (section_pending_counts from 10:00 batch = {وظائف: 4?} — wait section_pending_counts local to THIS drain invocation! It starts empty each call → for section not yet in counts → loop skips (for section in section_pending_counts) → returns False! BUG: at 14:00 run_pending, section_pending_counts is empty at start, first iteration: _cap_reached → for loop empty → False → consumes 1 item → queued=4; second iteration: counts {وظائف:1}, max_allowed=3, queued=4>=3 → break. So 1 extra call → total 5. FAIL.
FIX: include sections from queued articles too: iterate `set(section_pending_counts) | {a['section'] for a in self._queue if status queued and section fixed slots}` OR initialize section_pending_counts with queued counts: `for a in self._queue: section_pending_counts[a['section']] = section_pending_counts.get(...)+1` BUT careful: then queued_now (which counts ALL queued incl. these) >= queued_initial+new... cap check queued_now >= max_allowed: if initial=3, max=3 → reached immediately ✓ break before consume ✓ total 4 ✓.
Simplest correct: seed section_pending_counts with current queued counts per slotted section before loop:
```
queued_initial = {}
for a in self._queue:
    if a.get("status") == "queued" and self._get_section_fixed_slots(a.get("section", "")):
        queued_initial[a.get("section", "")] = queued_initial.get(..., 0) + 1
section_pending_counts = dict(queued_initial)
```
Then queued_now = queued_initial[section] + this_batch[section] counts correctly.
Also: max_allowed at 10:00 = 4 → initial queued=0, after 4 recovers queued_now=4>=4 → break on next iter ✓.
Run harness after this fix.

## Full suite results after InMemoryDB fix

133 run, 2 errors + 1 fail:
1. ERROR test_only_one_article_per_cycle + test_skips_past_slot_window_no_burst: line 424 `ch_config = next((c for c in channels ...)` TypeError channels=None not iterable. The P28 block in _process_queue calls self._process_slots(channels) and when it returns (False,'...') with channels passed as None AND action None → falls through to generic path where channels=None → crash. FIX: `_process_queue(self, channels: List[Dict] = None)` — when channels None, fetch self.db.get_all_channels(). The two integration tests call _process_queue() with no channels arg. Original (pre-P28) code: how did channels get set? Probably `channels = self.db.get_all_channels()` at top of _process_queue or _process_slots. Check the original zip (p28 copy) — but P28 scheduler IS the working code from earlier sessions; these 2 tests passed previously (133/133 per history). The current scheduler.py must have lost the `channels = channels or self.db.get_all_channels()` normalization. Fix: at start of _process_queue, `if channels is None: channels = self.db.get_all_channels()`. Also do same in _process_slots/channels usage.
2. FAIL test_gemini_pending_drains_progressively: expects 5 = PROGRESSIVE_DRAIN_BATCH, got 4. My new cap: pending items have channel_id ch1, section "general"; _get_section_fixed_slots("general") → does general have fixed slots? P28_SECTION_FIXED_SLOTS = {"وظائف": [...]} → general has none → cap not applied. Hmm then why 4 not 5? limit = min(max_batch=5,...) = 5. _cap_reached: section_pending_counts seeded with queued articles for sections WITH fixed slots (none for general) → empty → cap False. Should process 5. Got 4 → something limited it to 4. Maybe section_pending_counts seed counted general with fixed slots? No. OR: at seed, _get_section_fixed_slots("general") returns [] → skipped. Counts empty. Limit 5. processed<5... after 4th item: pending refreshed... maybe pending list shrinks by something else: remove_pending_by_fingerprint? No. Or the recovered() side_effect uses calls[0] counter — 4 calls = 4 recovered. Then 5th: pending still has items... while loop: pending refreshed = db queue minus popped items. _process_next_pending pops index 0. 5th call should happen... UNLESS cap: section_pending_counts includes general? Check my seed loop: for _a in self._queue — _queue starts [] ✓. Then after each recover, append; section_pending_counts['general']=+1. max_allowed: _section_max_allowed('general'): section_remaining has? keys from 'for section in section_remaining' — section_pending_counts keys... wait: section_remaining built earlier in method (before cap). It comes from: `for section in self._get_section_fixed_slots(...)`? Need to check _process_gemini_pending body L240-269. Maybe section_remaining built with ALL sections including general with len(slots)=0 → min includes 0 → limit=min(5, max(1, min(...))) ... if min over sections includes 0 then max(1,min) = 1 → limit 1?? But we got 4. Hmm. If section_remaining={general: 0, وظائف:4} → min=0, max(1,0)=1, limit=1. Got 4 though... Let me just read the code.
Fix approach: read L240-270, then decide.
