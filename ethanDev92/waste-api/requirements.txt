fastapi==0.115.0
uvicorn[standard]==0.31.0
pydantic==2.9.2
python-multipart==0.0.12
onnxruntime==1.19.2
Pillow==10.4.0
numpy==1.26.4
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0

# user_uploads 수집·피드백
supabase==2.7.4
python-dotenv==1.0.1

# MediaPipe Hands — predict-centered, predict-with-regions 에서 손 영역 검출 후 제외
mediapipe==0.10.18
opencv-python==4.10.0.84

# 시맨틱 증거 — 이미지 텍스트(분리배출 마크·정체어) OCR (SEMANTIC_FUSION_PLAN 신호①)
# onnxruntime 엔진만 사용 (torch 불요). 모델은 models/ocr/ 에 번들 — 런타임 다운로드 없음
rapidocr==3.9.1

# VLM 폴백 (트랙 A2) — 저확신 케이스 Claude 위임 (키 없으면 자동 비활성)
anthropic==0.117.0

# 서빙 모델 다운로드 (Dockerfile · scripts/fetch_models.py)
huggingface_hub>=0.24
