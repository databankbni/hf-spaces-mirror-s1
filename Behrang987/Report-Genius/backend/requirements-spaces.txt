# Hugging Face Spaces runtime — OpenAI embeddings only (no local torch / ST / Jina).
# Local embedder + local reranker code paths remain in the repo but are not installed
# here; set EMBEDDING_PROVIDER=openai and USE_RERANKER=false on the Space.

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# >=2.0.0: OpenAI Structured Outputs via client.chat.completions.parse
openai>=2.0.0
# Gemini Structured Outputs for evaluation judges
google-genai>=1.0.0

psutil>=5.9.0
langchain-openai>=0.2.0
langchain-core>=0.3.0
langchain-community>=0.3.0

faiss-cpu>=1.8.0
numpy>=1.26.0

python-docx>=1.1.0
pypdf>=4.0.0
pymupdf>=1.24.0

python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

pydantic>=2.7.0
pydantic-settings>=2.3.0

tiktoken>=0.7.0
