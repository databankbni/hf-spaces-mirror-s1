# Speech-to-text (local, no API key needed). faster-whisper ships prebuilt
# wheels (CTranslate2 backend) — no compiler/torch needed for ASR.
faster-whisper>=1.0.0

# Semantic retrieval
sentence-transformers>=3.0.0

# Lexical fuzzy matching
rapidfuzz>=3.9.0

# Microphone capture (CLI use)
sounddevice>=0.4.6
numpy>=1.24.0

# Web API server (consumed by the Next.js UI)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Claude-powered natural-language Q&A
anthropic>=0.40.0

# Accounts & billing
stripe>=10.0.0
PyJWT[crypto]>=2.8.0
requests>=2.31.0
# Supabase Postgres driver (SQLite fallback used when DATABASE_URL is unset)
psycopg[binary]>=3.1.0
