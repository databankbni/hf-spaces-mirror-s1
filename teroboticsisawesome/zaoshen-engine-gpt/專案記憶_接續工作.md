# 造神引擎GPT版｜專案記憶與接續工作

最後更新：2026-07-21（Asia/Taipei）

## 2026-07-21 🚨重大變更：已搬上 Google Cloud Run（真 SaaS，粉專發文修好）— GPT 接手必讀

### 為什麼搬（粉專發文一直 ReadTimeout 的真因）
- **不是程式 bug，是 HF Space 對 Facebook 的網路出口被擋。** 用自建的 `/api/fb/diag` 在 Space 內實測證明：IPv4 TCP「假連」0.002s（本地閘門）、GET/POST 到 graph.facebook.com 全 ReadTimeout；同時 OpenAI 從 HF 通 → 代表 FB 被 HF 針對性擋/限流。**重試、查重再多都沒用**（之前一直加重試是修錯層）。
- 對照：同 token 同呼叫從一般網路（WSL）與 Cloud Run 都能秒發+刪。

### 現在正式站 = Cloud Run（HF 那個 Space 退居備援，可退役）
- **正式網址：https://zaoshen-engine-v5eyyxbevq-de.a.run.app**（＝ https://zaoshen-engine-720131287666.asia-east1.run.app ）
- GCP 專案 `zaoshen-engine-app`（number 720131287666），帳號 **tonyangfactoryc@gmail.com**，region **asia-east1**，service **zaoshen-engine**。billing 綁 013E13（免費額度內 ≈$0，min-instances=0 縮到 0）。
- **從 Cloud Run 發 FB 已實測成功**（HF 發不出去的，Cloud Run 發得出去）。登入 wayne / partnera/b/c，密碼 1234（⚠️該改）。

### 資料庫持久化：改用 GCS（取代 HF private Dataset）
- bucket：`gs://zaoshen-engine-app-data`，物件 `saas.db`。
- `backend.py` 的 `restore_space_database()` / `database_backup_loop()` 已加 GCS 分支，由 env `ZAOSHEN_GCS_BUCKET` 觸發：開機從 GCS 還原、每 30 秒有變更就備份回 GCS。
- 現有資料（1 活動、107 內容、88 名單）已從 HF Dataset 搬到 GCS，實測還原正常。
- Cloud Run 執行 SA（720131287666-compute@developer）已授 bucket 的 `roles/storage.objectAdmin`。

### 🔧 GPT 以後怎麼部署/更新（改完是上 **Cloud Run**，不是上 HF！）
```bash
export PATH="$HOME/google-cloud-sdk/bin:$PATH"
# 用 ADC 換 access token（~/.config/gcloud/application_default_credentials.json 的 refresh_token）
AT=$(python3 -c "import json,urllib.request,urllib.parse;a=json.load(open('$HOME/.config/gcloud/application_default_credentials.json'));d=urllib.parse.urlencode({'client_id':a['client_id'],'client_secret':a['client_secret'],'refresh_token':a['refresh_token'],'grant_type':'refresh_token'}).encode();print(json.load(urllib.request.urlopen('https://oauth2.googleapis.com/token',data=d))['access_token'])")
export CLOUDSDK_AUTH_ACCESS_TOKEN="$AT" CLOUDSDK_CORE_DISABLE_PROMPTS=1
gcloud run deploy zaoshen-engine --source '/mnt/c/Users/HP-PC/Documents/WAYNE共享專案/造神引擎GPT版' \
  --region asia-east1 --allow-unauthenticated --project zaoshen-engine-app \
  --memory 1Gi --port 8080 --timeout 300 --max-instances 3
```
- 更新程式**不用重設 env**（env 已在 service 上；要改才 `--update-env-vars`）。**資料在 GCS，部署不會蓋掉。**
- `.gcloudignore` 已排除 stale `data/saas.db`／exe／build，**別刪**。Dockerfile CMD 已改 `${PORT:-7860}`（HF 7860／Cloud Run 8080 兩邊相容）。

### env（已設在 Cloud Run service，別重設漏掉）
`ZAOSHEN_GCS_BUCKET=zaoshen-engine-app-data` / `ZAOSHEN_LIVE=1` / `FB_PAGE_ID` / `FB_PAGE_TOKEN` / `OPENAI_API_KEY` / `SESSION_SECRET` / `ZAOSHEN_BOOTSTRAP_PASSWORD=1234`

### 發文邏輯改動（backend.py send_item）
- 強制 IPv4（HF IPv6 死的殘留防護）；帶圖在 Cloud Run 走 **multipart 直接上傳**（`public_media_url` 在 Cloud Run 回空→走上傳；只有 HF 才用 `/media` URL 讓 FB 抓）；重試退避 4 次；查重支援帶圖（caption 前 60 字）。
- 新增 `/api/fb/diag`（連線診斷，登入後可打）、`/api/queue/{qid}/mark-sent`（本機發文器回報用；Cloud Run 已不需本機發文器）。

### 🔴 待辦（GPT 要知道）
1. **密碼還 1234** → 該改（有 change-password 流程）。
2. **新上傳的圖沒持久化**：`data/uploads` 在 Cloud Run 是暫存，重啟會掉新上傳的圖（現有的圖 bundle 在 image 裡 OK）→ 要把 uploads 也接 GCS 才 100% 完整。
3. FB token/OpenAI key/SESSION_SECRET 現放 env（console 可見）→ 建議改 **Secret Manager**。
4. **私訊(pages_messaging)**：目前粉專 token 只有 pages_manage_posts/read_engagement，**不能讀/回私訊**。系統用戶也沒被指派此權限。要做「收單/自動回私訊」需在 **Meta 商業設定**把系統用戶加「管理訊息(MODERATE)」+ App 加 Messenger + 重產含 pages_messaging 的 token（同當初發文 token 流程）。**這步要 Wayne 在網頁做，API 加不了。**
5. HF Space（zaoshen-engine-gpt）可退役或留備援；本機發文器 `local_page_poster.py` 不再必要。

### 鐵律（不變）
不碰 ERP/Firebase/DNS；token/密鑰不寫進專案檔（放 env/Secret Manager）；含「待填/你的名字/您的名字」佔位符不得自動發布；正式發布前每篇人工審核。

---

## 2026-07-20 正式上線最新狀態

- Wayne 已明確授權部署到獨立 Hugging Face Space：`teroboticsisawesome/zaoshen-engine-gpt`。
- 正式網址：`https://teroboticsisawesome-zaoshen-engine-gpt.hf.space/`。
- HF 正式環境已設 `ZAOSHEN_LIVE=1`；使用者認領後，可由 Page Token 或個人 RPA 依排程執行。
- 含「待填」、「您的名字」、「你的名字」等佔位符的文案必須阻擋，不得自動發布。
- 正式 SQLite 已使用私有 Dataset `teroboticsisawesome/zaoshen-engine-data` 持久化；Space 啟動時還原，異動後定時備份。
- HF Secrets 已設定 Session、正式登入與 OpenAI 所需值；不得將密鑰明文寫進專案。
- 粉專「加州好日子 California Days」Page ID 為 `371027582769797`；Page Token 來源為專案外安全檔 `~/californiadays_fb/secrets/page_token.txt`，已寫入 HF Secret `FB_PAGE_TOKEN`，不得將內容複製進專案。
- 正式 `/api/fb/status` 已驗證 `connected=true`；權限為 `pages_manage_posts` 與 `pages_read_engagement`。
- 個人 RPA 最新版本為 `1.2.1`，網站預設為正式 HF 網址。
- RPA 1.2.0 曾因 Tkinter `.pack(pad=...)` 非法參數在啟動時崩潰；1.2.1 已改為 `.pack(padx=...)`。
- 正式下載檔：`dist-installer/造神引擎RPA安裝程式-1.2.1.exe`，線上與本機 SHA-256 已核對一致。
- 新增原創加州風格視覺：太平洋浪、落日、棕櫚與能量閃電品牌標誌，並以專屬 SVG 取代常見 emoji/AI icon。
- 品牌資產位於 `static/brand/`，主標誌為 `california-emblem.png`，圖示系統為 `icons.svg`。
- 不得操作 ERP、Firebase 或 DNS，除非 Wayne 另外明確逐次授權。

## 最高優先規則

- 本專案是獨立網站「造神引擎GPT版」。
- ERP 是禁區。沒有 Wayne 明確、逐次同意，不得讀取、修改、部署或串接 ERP。
- 同樣不得擅自操作 Firebase、DNS。
- Wayne 已授權部署本專案到 Hugging Face Space；目標僅限 `teroboticsisawesome/zaoshen-engine-gpt`，不是 ERP 空間。
- 不得把密碼、Facebook Cookie、裝置 token、API key 或 HF_TOKEN 寫入專案檔案。
- Wayne 已授權本專案使用 `ZAOSHEN_LIVE=1`；仍必須保留佔位符、未核准與缺憑證阻擋。

## 專案位置

- Windows：`C:\Users\HP-PC\Documents\WAYNE共享專案\造神引擎GPT版`
- WSL：`/mnt/c/Users/HP-PC/Documents/WAYNE共享專案/造神引擎GPT版`
- 部署手冊：`/mnt/c/Users/HP-PC/Documents/WAYNE共享專案/部署手冊_給GPT.md`

## 產品與權限共識

- Wayne 與 GPT 是系統管理方；GPT 只能在 Wayne 要求及授權時操作。
- 夥伴 A、B、C 共同經營同一個活動，不是各自擁有獨立活動。
- 所有夥伴都可看到全局：活動數據、全部內容、排程、任務、其他夥伴工作進度和裝置狀態。
- 所有夥伴都能共同建立／修改活動、產生 GPT 文案、修改稿件、核准／退回、派工及下載 RPA。
- 共同公開的是工作資訊；私人資訊必須隔離。
- 私人資訊包括：登入密碼、Facebook 密碼及 Cookie、RPA 裝置 token、API keys、平台 secrets、個人電腦檔案。

## 已完成

### 原有行銷系統

- FastAPI、SQLite 與靜態 HTML 架構。
- 活動管理者總覽 `/manager`。
- 3D 戰情室 `/warroom`，102 個內容節點。
- 3D 節點旋轉、拖曳視角、滾輪縮放、點擊內容抽屜。
- 圖片節點標示、縮圖、完整圖片及只有圖片篩選。
- 內容中心 `/`、名單與留言頁 `/audience`。
- 活動排程、審核佇列、核准、派工、粉專 Graph API 與本機 RPA 任務架構。
- 現有資料：1 個活動、102 個內容、88 個目標（55 社團、33 KOL）。
- 安全模式確認為 `live=false`。

### 登入與共同活動

- `auth.py`：PBKDF2 密碼雜湊、簽章 session、裝置 token 雜湊。
- 使用者資料表、共同活動成員資料表、裝置、配對碼、操作紀錄資料表。
- 登入頁 `/login`。
- 未登入的頁面會轉到登入頁；API 回應 401。
- 本機測試帳號：`wayne`、`partnera`、`partnerb`、`partnerc`。
- 四個帳號目前的本機測試密碼都是 `1234`；舊密碼已失效。
- 正式環境若沒有 `ZAOSHEN_BOOTSTRAP_PASSWORD` 與 `SESSION_SECRET`，後端會拒絕啟動，避免測試密碼上線。

### 夥伴與 RPA 裝置

- 團隊頁 `/team`。
- 顯示所有夥伴與裝置：在線、待命、執行中、異常、離線、版本、作業系統、目前任務、最後心跳及錯誤。
- RPA 每 20 秒回報心跳；超過 65 秒顯示離線。
- 每位夥伴可產生 8 碼、10 分鐘有效、只能使用一次的配對碼。
- 配對完成後裝置 token 只存夥伴電腦，不在共同頁面公開。
- 支援裝置撤銷與操作稽核紀錄。
- RPA 工作領取與結果回報都要求 Bearer 裝置 token。
- RPA 使用 Windows 內建 Microsoft Edge，降低 Chrome／驅動版本差異。
- Facebook profile、Cookie、截圖與裝置設定存於 `%LOCALAPPDATA%\ZaoshenRPA`。
- RPA 控制視窗支援配對、啟動、停止及狀態顯示。

### Windows EXE 打包工程

已建立：

- `rpa_control.py`
- `local_worker.py`
- `ZaoshenRPA.spec`
- `installer.iss`
- `打包Windows安裝版.ps1`
- `打包Windows安裝版.bat`

設計目標：

- 使用 PyInstaller 將 Python、Playwright 及程式封裝成 `造神引擎RPA.exe`。
- 使用 Inno Setup 產生 `造神引擎RPA安裝程式-1.0.0.exe`。
- 夥伴電腦不需要安裝 Python。
- 網站 `/api/rpa/download` 偵測正式安裝程式存在後，會直接提供 EXE；尚未產生時才提供開發 ZIP。

## 已驗證

- Python 語法檢查通過：`backend.py`、`auth.py`、`local_worker.py`、`rpa_control.py`。
- 所有 HTML 內嵌 JavaScript 語法檢查通過。
- 四個帳號皆可用密碼 `1234` 驗證，舊密碼不可用。
- 未登入 API 阻擋設計完成。
- 夥伴 A 配對流程、裝置 token、心跳、在線／待命狀態驗證通過。
- RPA ZIP 包含控制器、worker、啟動檔及說明。
- 測試裝置已清除，資料庫沒有殘留「驗收測試電腦」。
- 102 個內容資料仍存在，安全模式沒有開啟正式發布。

## 尚未完成／目前阻擋

### Windows 安裝程式尚未實際產生

目前 Codex 工作階段無法啟動 Windows 程式，執行 `powershell.exe` 會出現：

`WSL ERROR: UtilBindVsockAnyPort: socket failed 1`

因此打包腳本與規格已完成，但目前檔案系統中還沒有：

`dist-installer\造神引擎RPA安裝程式-1.0.0.exe`

需要在 Windows 桌面直接雙擊：

`打包Windows安裝版.bat`

成功後必須再驗證：

1. 安裝程式能在沒有 Python 的 Windows 10／11 電腦安裝。
2. RPA 控制視窗能開啟。
3. 配對碼能完成綁定。
4. 啟動後 20 秒內網站顯示在線。
5. 停止 65 秒後顯示離線。
6. Edge 能開啟且 Facebook Cookie 只保留本機。
7. EXE 版本與網站最低支援版本一致。
8. 對外發送前需處理 Windows 程式碼簽章；未簽章版本可能觸發 SmartScreen。

### 其他後續功能

- 正式的帳號建立、停用、改密碼及忘記密碼介面。
- 工作指派／接手的完整 UI 與每位夥伴工作量統計。
- RPA 自動更新機制與版本過舊停止派工。
- 真實留言及成效同步，目前不能假造數據。
- 正式部署前的權限、安全、secret 與全流程驗收。
- 尚未獲准重新部署 Hugging Face。

## 本機啟動與測試

雙擊：`啟動本機預覽.bat`

或在 Ubuntu 執行：

```bash
cd '/mnt/c/Users/HP-PC/Documents/WAYNE共享專案/造神引擎GPT版'
python3 -m uvicorn backend:app --host 0.0.0.0 --port 8800
```

開啟：`http://127.0.0.1:8800/login`

詳細驗收步驟見：`本機驗收說明.md`

## 已知線上版本

先前 Claude 部署的 Hugging Face Space：

- 控制台：`https://teroboticsisawesome-zaoshen-engine-gpt.hf.space/`
- 3D 戰情室：`https://teroboticsisawesome-zaoshen-engine-gpt.hf.space/warroom`

注意：線上版本可能仍是舊版。沒有 Wayne 明確下令，不得重新部署。

## 接手者第一步

1. 完整閱讀本文件。
2. 確認 Wayne 當次授權範圍。
3. 不碰 ERP、Firebase、DNS。
4. 若任務是 EXE 打包，先確認 `dist-installer` 是否已有安裝程式；沒有就從 Windows 執行 `打包Windows安裝版.bat`。
5. 若任務是本機測試，保持 `ZAOSHEN_LIVE=0`。
6. 若任務是部署，必須先得到 Wayne 明確同意，並再次閱讀部署手冊。
