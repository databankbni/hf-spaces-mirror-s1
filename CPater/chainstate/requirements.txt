# CHAINSTATE swarm-node Python requirements.
# Install: pip install -r requirements.txt

# ── core ─────────────────────────────────────────────────────────
torch>=2.2.0
numpy>=1.26.0
scipy>=1.12.0

# ── HTTP / serving ───────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.6.0

# ── storage ──────────────────────────────────────────────────────
redis>=5.0.0
qdrant-client>=1.8.0

# ── web3 / chain ─────────────────────────────────────────────────
web3>=6.15.0
eth-account>=0.11.0

# ── crypto ───────────────────────────────────────────────────────
pycryptodome>=3.20.0
pynacl>=1.5.0
# CRYSTALS-Dilithium / Kyber bindings are loaded via the NWO security envelope;
# install from your private wheel index, e.g.:
#   pip install pq-crystals --extra-index-url <nwo-internal>

# ── quantum (optional, for nodes that advertise capability=quantum) ──
qiskit>=1.0.0
qiskit-ibm-runtime>=0.20.0
# Origin Wukong: install the vendor-supplied client from
#   pip install origin-quantum-client

# ── transformers (used in some symbolic preprocessing) ───────────
transformers>=4.40.0
tokenizers>=0.19.0

# ── observability ────────────────────────────────────────────────
prometheus-client>=0.20.0
structlog>=24.1.0
