phase: phase1_mvp
hf_free_mode: true
sources:
  titan007:
    role: ["hot_match_pool_external_seed", "odds_primary", "match_id"]
    migrated: true
    blocking: true
  multi_company_analyzer:
    role: ["ADI", "SFI", "convergence_bonus"]
    migrated: true
    blocking: false
  kline_compact:
    role: ["water_kline_summary"]
    migrated: true
    blocking: false
  crow_screener:
    role: ["crow_hot_batch"]
    migrated: false
    reason: "Playwright/Chromium dependency; keep optional fallback"
  open_meteo:
    role: ["weather"]
    migrated: false
    target_phase: phase1_optional_when_venue_coords_exist
  football_data_org_or_api_football:
    role: ["identity_cross_check", "standings", "fixtures"]
    migrated: false
    target_phase: phase1b
boundaries:
  final_pick: "Hermes local only"
  bankroll_records: "Hermes local only"
  MATCH_TRACKER_MASTER: "Hermes local only"
  case_memory: "Hermes local only"
