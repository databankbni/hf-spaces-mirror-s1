"""
utils.py — тонкий реэкспорт-модуль.

Раньше здесь был один файл на 1255+ строк и 38 функций. Логика разбита на модули по
смыслу (см. ниже), а этот файл оставлен как единая точка входа, чтобы не пришлось
переписывать импорты во всех остальных файлах (scan_processing.py, work_processing.py,
handlers.py и т.д. — они как использовали `utils.имя_функции`, так и продолжают).

    text_helpers.py — общие текстовые хелперы (unicode-safety, нормализация, fuzzy)
    ocr.py          — распознавание: нативное извлечение, Gemini OCR, детекция подписи
    metadata.py      — извлечение/нормализация метаданных работ
    pdf_tools.py    — манипуляции с PDF-файлами

Если нужно добавить новую функцию — добавляйте её в соответствующий тематический модуль,
а не сюда.
"""

from text_helpers import *
from ocr import *
from pdf_tools import *
from metadata import *

ALLOWED_WORK_TYPES = ["ЛР", "ПЗ", "КР", "КП", "ИЗ", "СРС", "ДКР", "СР", "ПР", "УП", "ПП"]