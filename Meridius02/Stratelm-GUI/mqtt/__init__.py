"""Antasena MQTT telemetry pipeline (logger, cleaner, lap tools, field
simulator) -- migrated from the standalone MQTT project into this repo so
Stratelm-GUI's backend can import the cleaner directly (see
Stratelm-GUI/backend/telemetry_capture.py) instead of shelling out.
"""
