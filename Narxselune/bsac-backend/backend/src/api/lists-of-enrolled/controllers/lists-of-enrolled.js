'use strict';

/**
 * lists-of-enrolled controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::lists-of-enrolled.lists-of-enrolled');
