'use strict';

/**
 * lists-of-enrolled service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::lists-of-enrolled.lists-of-enrolled');
