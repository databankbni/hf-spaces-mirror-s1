<%= turbo_stream.replace "favorites", partial: "weather/favorites" %>
<%= turbo_stream.replace "favorite_header_button", partial: "weather/favorite_header_button" %>

<% flash.each do |type, message| %>
  <%= turbo_stream.append "flash_container", partial: "layouts/flash", locals: { type: type, message: message } %>
<% end %>
