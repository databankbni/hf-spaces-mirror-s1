# Runtime dependencies only. The heavy geospatial / NetCDF stack
# (geopandas, rasterio, fiona, shapely, xarray, netCDF4) was used only by
# offline data-prep scripts, NOT by the running app — removing them keeps the
# HuggingFace Docker build fast (avoids GDAL compilation → no build timeout).
dash==2.17.1
dash-bootstrap-components==1.6.0
plotly==5.22.0
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
pymannkendall==1.4.3
statsmodels==0.14.2
diskcache==5.6.3
gunicorn==22.0.0
pyarrow==16.1.0
matplotlib>=3.6
certifi

huggingface_hub>=0.20
