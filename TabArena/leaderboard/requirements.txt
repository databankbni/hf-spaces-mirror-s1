APScheduler
gradio[mcp,oauth]==6.22.0
gradio_client
# Pinned deliberately: unpinned, a rebuild now picks up pandas 3, which changes
# string columns from `object` to `str` dtype. Bump it on its own, not alongside
# an unrelated change.
pandas==2.3.3
python-dateutil
